import React, {useState} from 'react'

function ItemCounter ({productsCounter}) {
  const [] = useState()
  return (
    <div>
        <p>{productsCounter}</p>
    </div>
  )
}

export default ItemCounter